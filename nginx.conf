sed -i 's/http {/http {\n    client_max_body_size 100M;/' /nginx.conf && sed -i 's|location ~ \.php$|location / {\n        try_files $uri $uri/ $uri.html =404;\n    }\n\n    location ~ \.php$|' /nginx.conf && nginx -s reload
